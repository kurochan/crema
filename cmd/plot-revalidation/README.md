# plot-revalidation

Generates an SVG plot of the revalidation probability curves.

## Usage

```sh
go run ./cmd/plot-revalidation
```

The command writes `revalidation.svg` to the current working directory.
